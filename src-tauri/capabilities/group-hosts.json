{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "group-hosts",
  "description": "Secondary group hosts have no updater or opener permissions.",
  "windows": ["group-*"],
  "permissions": ["core:default"]
}
