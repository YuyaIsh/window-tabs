{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "group-hosts",
  "description": "Secondary group hosts have no updater, process, or opener permissions.",
  "windows": ["group-*"],
  "permissions": ["core:default"]
}
