{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Controller-only permissions, including update ownership.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "opener:default",
    "updater:default"
  ]
}
